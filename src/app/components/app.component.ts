import { Component } from '@angular/core';

@Component({
    selector: 'my-app',
    styleUrls: ['app/content/app.css'],
    templateUrl: `app/templates/app.html`
})
export class AppComponent {
    name = 'Angular';
}
